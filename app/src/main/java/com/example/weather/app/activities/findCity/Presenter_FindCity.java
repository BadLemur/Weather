package com.example.weather.app.activities.findCity;

import com.arellomobile.mvp.MvpPresenter;

public class Presenter_FindCity extends MvpPresenter<View_FindCity> {

    @Override
    public void attachView(View_FindCity view) {
        super.attachView(view);
    }
}